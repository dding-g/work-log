# ============================================================
# Work-Log Automation 의존성 목록
# ============================================================
#
# 설치 방법: pip install -r requirements.txt
#

# ----- 필수 (Required) -----

# Pydantic: 데이터 검증 및 설정 관리
pydantic>=2.5.0

# httpx: 비동기 HTTP 클라이언트 (GitHub API, Slack 연동)
httpx>=0.25.0

# boto3: AWS SDK (S3 연동)
boto3>=1.34.0


# ----- LLM 연동 -----

# OpenAI: GPT 모델 사용 시 필요
openai>=1.3.0

# Anthropic: Claude 모델 사용 시 필요
anthropic>=0.7.0


# ----- 로컬 개발용 (Local Development) -----

# FastAPI: 웹 API 프레임워크 (로컬 테스트용)
fastapi>=0.104.0

# Uvicorn: ASGI 서버 (로컬에서 FastAPI 실행)
uvicorn[standard]>=0.24.0

# Mangum: AWS Lambda에서 FastAPI 실행을 위한 어댑터
mangum>=0.17.0

# GitPython: Git을 파이썬으로 다루는 라이브러리 (로컬용)
gitpython>=3.1.40
