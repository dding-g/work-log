# ============================================================
# Work-Log Automation 의존성 목록
# ============================================================
#
# 설치 방법: pip install -r requirements.txt
#

# ----- 필수 (Required) -----

# FastAPI: 웹 API 프레임워크
fastapi>=0.104.0

# Pydantic: 데이터 검증 및 설정 관리
pydantic>=2.5.0

# GitPython: Git을 파이썬으로 다루는 라이브러리
gitpython>=3.1.40

# Mangum: AWS Lambda에서 FastAPI 실행을 위한 어댑터
# Lambda 환경에서 API Gateway 이벤트를 ASGI로 변환
mangum>=0.17.0


# ----- 로컬 개발용 (Local Development) -----

# Uvicorn: ASGI 서버 (로컬에서 FastAPI 실행)
# Lambda에서는 불필요하지만 로컬 테스트에 필요
uvicorn[standard]>=0.24.0


# ----- LLM 연동 (선택) -----

# OpenAI: GPT 모델 사용 시 필요
# 설치: pip install openai
# openai>=1.3.0

# Anthropic: Claude 모델 사용 시 필요
# 설치: pip install anthropic
# anthropic>=0.7.0


# ----- 개발/테스트 (Development) -----

# pytest: 테스트 프레임워크
# pytest>=7.4.0

# httpx: 비동기 HTTP 클라이언트 (테스트용)
# httpx>=0.25.0
