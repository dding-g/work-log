# ============================================================
# Lambda 배포용 최소 의존성
# ============================================================
#
# Lambda ZIP 패키지 크기를 최소화하기 위한 의존성 목록
# boto3는 Lambda 런타임에 기본 포함되어 있어 제외
#

# 데이터 검증
pydantic>=2.5.0

# HTTP 클라이언트 (GitHub API, Slack 연동)
httpx>=0.25.0

# LLM 연동
openai>=1.3.0
anthropic>=0.7.0
